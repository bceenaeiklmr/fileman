# fileman
A duplicate file finder built in **AutoHotkey v2**. It utilizes multi-threaded hashing via child worker processes, the native Windows Crypto API (`bcrypt.dll`), and COM-based inter-process communication to process backup archives in minutes.
